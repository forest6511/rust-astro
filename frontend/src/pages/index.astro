---
// src/pages/index.astro
import Layout from '@/layouts/Layout.astro'
import MainContent from '@/components/MainContent'
import { getPageTitle, getPageDescription } from '@/lib/data-utils'

// すべてのカテゴリのタイトルと説明を取得
const pageTitle = getPageTitle(null);
const pageDescription = getPageDescription(null);
---

<Layout title={pageTitle} description={pageDescription}>
  <MainContent client:load initialCategoryId={null} />
</Layout>